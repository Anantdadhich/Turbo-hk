import express from "express"
const app=express()
import {VALUE}
 from "@repo/common/ config"
console.log(URL)

app.get("/", (req,res)=>{
    res.json({
        messge:"helloo oo"
    })
})

app.listen(3000);